# 실제 학습에 사용되는 피처 상세 설명

이 문서는 본 프로젝트의 주식 가격 예측 모델(`RandomForestClassifier`) 학습에 사용되는 **25개**의 피처(Feature)에 대한 상세 설명을 제공합니다. 각 피처의 출처(Source) 및 계산식(Calculation)을 명시하여 투명성을 높였습니다.

---

## 1. 재무 및 가치 지표 (Fundamental & Value Indicators)

기업의 내재 가치와 재무 상태를 평가하는 지표들입니다.

| 피처명 (Feature) | 설명 | 출처 / 계산식 |
| :--- | :--- | :--- |
| `PBR` | **주가순자산비율** (Price to Book-value Ratio) | 기업의 자산 대비 주가 수준을 나타냅니다. 1보다 낮으면 주가가 장부상 가치보다 낮다는 의미일 수 있습니다. | `pykrx` API (`stock.get_market_fundamental`) |
| `log_mktcap` | **시가총액 (로그 변환)** | 기업 규모를 나타내는 시가총액에 자연로그를 취한 값입니다. 데이터 분포를 정규화하여 모델 안정성을 높입니다. | `np.log(시가총액)` <br/> (시가총액: `fdr.StockListing('KRX-MARCAP')`) |
| `이익수익률` | **이익수익률** (Earnings Yield) | 주가 대비 기업의 순이익 비율입니다. PER의 역수이며, 채권 수익률 등과 직접 비교하는 데 사용됩니다. | `1 / PER` <br/> (PER: `pykrx` API) |
| `BPS` | **주당순자산** (Book-value Per Share) | 기업의 순자산을 총 발행 주식 수로 나눈 값으로, 기업의 재무 안정성을 나타냅니다. | `pykrx` API (`stock.get_market_fundamental`) |

## 2. 모멘텀 및 변동성 지표 (Momentum & Volatility Indicators)

주가의 추세, 변화율, 변동 수준을 측정하는 지표들입니다.

| 피처명 (Feature) | 설명 | 출처 / 계산식 |
| :--- | :--- | :--- |
| `수익률(1M)` | **1개월 수익률** | 최근 1개월(20 거래일) 동안의 주가 등락률입니다. 단기 모멘텀을 나타냅니다. | `df['종가'].pct_change(20)` |
| `수익률(3M)` | **3개월 수익률** | 최근 3개월(60 거래일) 동안의 주가 등락률입니다. 중기 모멘텀을 나타냅니다. | `df['종가'].pct_change(60)` |
| `52주_신고가_비율` | **52주 신고가 비율** | 현재 주가가 지난 52주(250 거래일) 최고가 대비 어느 수준인지를 나타냅니다. 1에 가까울수록 강한 상승 추세를 의미합니다. | `현재 종가 / df['종가'].rolling(250).max()` |
| `ADX_14` | **14일 평균 방향성 지수** (Average Directional Index) | 현재 주가 추세의 '강도'를 0에서 100 사이의 값으로 나타냅니다. 상승/하락 방향과 무관하게 추세가 강할수록 값이 높아집니다. | `pandas-ta` 라이브러리 (`df.ta.adx(length=14)`) |
| `변동성(1W)` | **1주일 변동성** | 최근 1주일(5 거래일)간의 주가 변동성을 측정합니다. 주가의 단기 안정성을 파악하는 데 사용됩니다. | `df['종가'].rolling(5).std() / df['종가'].rolling(5).mean()` |
| `변동성(1M)` | **1개월 변동성** | 최근 1개월(20 거래일)간의 주가 변동성을 측정합니다. | `df['종가'].rolling(20).std() / df['종가'].rolling(20).mean()` |
| `변동성(3M)` | **3개월 변동성** | 최근 3개월(60 거래일)간의 주가 변동성을 측정합니다. | `df['종가'].rolling(60).std() / df['종가'].rolling(60).mean()` |
| `ATRr_14` | **14일 평균 실제 범위** (Average True Range) | 주가의 일일 변동폭(고가-저가)에 전일 종가와의 갭(gap)을 반영한 변동성 지표입니다. | `pandas-ta` 라이브러리 (`df.ta.atr(length=14)`) |
| `BBW_20_2` | **20일 볼린저 밴드 폭** (Bollinger Band Width) | 볼린저 밴드의 상단과 하단 밴드 사이의 폭을 중간 밴드로 나눈 값입니다. 변동성의 상대적 크기를 나타냅니다. | `(상단밴드 - 하단밴드) / 중간밴드` <br/> (밴드: `pandas-ta` 라이브러리) |
| `BB_Position` | **볼린저 밴드 내 주가 위치** | 현재 주가가 볼린저 밴드 내에서 어느 위치에 있는지를 0(하단)에서 1(상단) 사이의 값으로 표현합니다. | `(현재 종가 - 하단밴드) / (상단밴드 - 하단밴드)` <br/> (밴드: `pandas-ta` 라이브러리) |
| `disparity_120` | **120일 이격도** | 현재 주가가 120일 이동평균선과 얼마나 떨어져 있는지를 나타내는 비율입니다. 중기 추세와의 괴리를 측정합니다. | `(현재 종가 / 120일 이동평균) * 100` |
| `disparity_240` | **240일 이격도** | 현재 주가가 240일 이동평균선과 얼마나 떨어져 있는지를 나타내는 비율입니다. 장기 추세와의 괴리를 측정합니다. | `(현재 종가 / 240일 이동평균) * 100` |

## 3. 거래량 관련 지표 (Volume-based Indicators)

거래량 데이터를 통해 시장의 관심도와 자금 흐름을 파악합니다.

| 피처명 (Feature) | 설명 | 출처 / 계산식 |
| :--- | :--- | :--- |
| `거래대금_MA5` | **5일 평균 거래대금** | 최근 5일간의 일일 평균 거래대금입니다. 단기적인 시장 참여 강도를 나타냅니다. | `(종가 * 거래량).rolling(5).mean()` |
| `거래대금_MA20` | **20일 평균 거래대금** | 최근 20일간의 일일 평균 거래대금입니다. 중기적인 시장 유동성 및 관심도를 나타냅니다. | `(종가 * 거래량).rolling(20).mean()` |
| `OBV` | **거래량 가중 주가 지수** (On-Balance Volume) | 주가가 상승한 날의 거래량은 더하고 하락한 날의 거래량은 빼서, 거래량의 힘을 통해 주가 추세를 예측하는 지표입니다. | `pandas-ta` 라이브러리 (`df.ta.obv()`) |

## 4. 거시 경제 지표 (Macroeconomic Indicators)

시장 전체의 상황과 투자 심리를 반영하는 외부 지표들입니다.

| 피처명 (Feature) | 설명 | 출처 / 계산식 |
| :--- | :--- | :--- |
| `KOSPI_pct_1d` | **코스피 1일 변화율** | 국내 주식 시장의 대표 지수인 코스피의 전일 대비 등락률입니다. | `KOSPI지수.pct_change(1)` <br/> (지수: `fdr.DataReader('KS11')`) |
| `KOSPI_pct_5d` | **코스피 5일 변화율** | 코스피 지수의 5일 전 대비 등락률입니다. | `KOSPI지수.pct_change(5)` <br/> (지수: `fdr.DataReader('KS11')`) |
| `USDKRW_pct_1d`| **원/달러 환율 1일 변화율** | 원/달러 환율의 전일 대비 등락률입니다. 외국인 자금 유출입에 영향을 줍니다. | `환율.pct_change(1)` <br/> (환율: `fdr.DataReader('USD/KRW')`) |
| `USDKRW_pct_5d`| **원/달러 환율 5일 변화율** | 원/달러 환율의 5일 전 대비 등락률입니다. | `환율.pct_change(5)` <br/> (환율: `fdr.DataReader('USD/KRW')`) |
| `VIX_pct_1d` | **VIX 지수 1일 변화율** | '공포 지수'로 불리는 VIX 지수의 전일 대비 등락률입니다. 시장의 위험 회피 심리를 나타냅니다. | `VIX지수.pct_change(1)` <br/> (지수: `fdr.DataReader('^VIX')`) |
| `VIX_pct_5d` | **VIX 지수 5일 변화율** | VIX 지수의 5일 전 대비 등락률입니다. | `VIX지수.pct_change(5)` <br/> (지수: `fdr.DataReader('^VIX')`) |

---

## ※ 목표 변수 (Target Variable)

| 변수명 (Variable Name) | 설명 | 계산식 |
| :--- | :--- | :--- |
| `target` | **15일 후 5% 상승 여부** | 이 변수는 모델이 예측하려는 **목표값(Label)**입니다. 현재로부터 15거래일 후의 종가가 현재 종가보다 5% 이상 상승했으면 1, 그렇지 않으면 0이 됩니다. 이 변수는 모델 학습 시 피처로 사용되지 않습니다. | `(df['종가'].shift(-15) / df['종가'] > 1.05).astype(int)` |
