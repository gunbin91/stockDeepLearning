# 피처(Features) 목록 및 설명

## 개요
이 문서는 주식 딥러닝 프로젝트에서 사용하는 모든 피처(Features)의 목록, 기능, 계산 방법을 정리한 것입니다.

**총 피처 수: 18개**

---

## 1. 기본 지표

### 1.1 log_mktcap (시가총액 로그)
- **역할**: 종목의 규모를 나타내는 지표. 로그 변환으로 왜도 완화
- **계산법**: `log(시가총액)`
- **의미**: 시가총액이 클수록 대형주, 작을수록 소형주
- **사용 라이브러리**: numpy

### 1.2 52주_신고가_비율
- **역할**: 현재가가 52주 최고가 대비 어느 정도 위치인지 측정
- **계산법**: `종가 ÷ 52주 최고가` (250일 롤링 최대값 사용)
- **의미**: 1.0에 가까울수록 신고가 근처, 낮을수록 하락세
- **사용 라이브러리**: pandas

---

## 2. 추세 지표

### 2.1 ADX_14 (Average Directional Index)
- **역할**: 추세의 강도를 측정하는 지표
- **계산법**: `ADX(14)` - pandas-ta 라이브러리 사용
- **의미**: 값이 높을수록 강한 추세, 낮을수록 횡보
- **사용 라이브러리**: pandas-ta

---

## 3. 이격도 지표

### 3.1 disparity_60 (60일 이격도)
- **역할**: 현재가가 60일 이동평균 대비 얼마나 떨어져 있는지 측정
- **계산법**: `(종가 ÷ 60일 이동평균) × 100`
- **의미**: 100보다 크면 이동평균 위, 작으면 아래
- **사용 라이브러리**: pandas

### 3.2 disparity_120 (120일 이격도)
- **역할**: 현재가가 120일 이동평균 대비 얼마나 떨어져 있는지 측정
- **계산법**: `(종가 ÷ 120일 이동평균) × 100`
- **의미**: 중장기 추세 판단에 사용
- **사용 라이브러리**: pandas

### 3.3 disparity_240 (240일 이격도)
- **역할**: 현재가가 240일 이동평균 대비 얼마나 떨어져 있는지 측정
- **계산법**: `(종가 ÷ 240일 이동평균) × 100`
- **의미**: 장기 추세 판단에 사용
- **사용 라이브러리**: pandas

### 3.4 KOSPI_disparity_60 (KOSPI 60일 이격도)
- **역할**: KOSPI 지수가 60일 이동평균 대비 얼마나 떨어져 있는지 측정
- **계산법**: `(KOSPI ÷ KOSPI 60일 이동평균) × 100`
- **의미**: 시장 전체의 중기 추세 판단
- **사용 라이브러리**: pandas

---

## 4. 거래량 지표

### 4.1 OBV_Slope (OBV 기울기)
- **역할**: 스텔스 매집 패턴 포착. 주가는 횡보하거나 눌리는데 OBV는 우상향하는 경우
- **계산법**: `(당일 OBV - 5일 전 OBV) ÷ 5일 전 OBV 절대값`
- **의미**: 값이 양수면 매집 진행 중, 음수면 분산 진행 중
- **참고**: OBV는 계산되지만 피처로는 사용하지 않음 (OBV_Slope 계산에만 사용)
- **사용 라이브러리**: pandas-ta (OBV 계산), pandas (기울기 계산)

---

## 5. 변동성 지표

### 5.1 ATRr_20 (ATR 비율 20일)
- **역할**: 하루 평균 움직임(%)을 측정. 기초 체력 지표
- **계산법**: pandas-ta가 `ATRr_20` 컬럼을 자동 생성 (이미 `(ATR(20) ÷ 종가) × 100` 형태)
- **의미**: 수치가 너무 낮으면 10일 내 8% 도달이 불가능하므로 필터링에 사용
- **사용 라이브러리**: pandas-ta

### 5.2 ATR_Ratio_Short (단기 응축)
- **역할**: 눌림목 포착. 평소(1달) 대비 이번 주(1주)가 얼마나 조용한가
- **계산법**: `ATRr_5 ÷ ATRr_20`
- **의미**: 수치가 낮을수록 에너지 응축(눌림), 상승 전조
- **사용 라이브러리**: pandas-ta (ATR 계산), pandas (비율 계산)

### 5.3 ATR_Ratio_Trend (에너지 추세)
- **역할**: 관심 집중도 측정. 과거(3달) 대비 요즘(1달)이 더 활발한가
- **계산법**: `ATRr_20 ÷ ATRr_60`
- **의미**: 수치가 1.0 넘으면 시장 관심 증가, 상승 모멘텀
- **사용 라이브러리**: pandas-ta (ATR 계산), pandas (비율 계산)

### 5.4 KOSPI_변동성(1M) (KOSPI 변동성 1개월)
- **역할**: KOSPI 지수의 1개월(20일) 변동성 측정
- **계산법**: `KOSPI.rolling(20).std() ÷ KOSPI.rolling(20).mean()`
- **의미**: 시장 전체의 변동성 수준 파악
- **사용 라이브러리**: pandas

---

## 6. 효율 지표

### 6.1 Eff_Ratio_10 (효율 비율 10일)
- **역할**: 손절 방어 지표. 주가가 지저분하게 흔들리지 않고 깨끗하게 움직이는지 측정
- **계산법**: `(10일 순수 상승폭 절대값) ÷ (10일 등락폭 합)`
  - 순수 상승폭: `종가.diff(10).abs()`
  - 등락폭 합: `종가.diff().abs().rolling(10).sum()`
- **의미**: 높을수록 안전한 상승, 낮을수록 불안정한 움직임
- **사용 라이브러리**: pandas

---

## 7. 수급 지표

### 7.1 RVOL (상대 거래량)
- **역할**: 상승 트리거 확인. 평소보다 거래량이 얼마나 폭발적으로 터졌는가
- **계산법**: `당일 거래량 ÷ 20일 평균 거래량`
- **의미**: 급등의 신호탄. 1.0보다 크면 평소보다 거래량 증가
- **사용 라이브러리**: pandas

---

## 8. 위치 지표

### 8.1 Z_Score_20 (표준화 이격 20일)
- **역할**: 과매도 판단. 단기 평균 대비 통계적으로 얼마나 싼가
- **계산법**: `(종가 - 20일 평균) ÷ 20일 표준편차`
- **의미**: -2.0 근처면 통계적 바닥(반등 임박), 0 근처면 평균 수준
- **사용 라이브러리**: pandas

### 8.2 Position_Range_60 (Donchian 위치)
- **역할**: 추세 위치 측정. 지난 분기(60일)의 고가/저가 사이에서 현재 어디쯤인가
- **계산법**: `(종가 - 60일 최저) ÷ (60일 최고 - 60일 최저)`
- **의미**: 0~1 범위, 1.0에 가까울수록 신고가 돌파 직전, 0에 가까울수록 신저가 근처
- **사용 라이브러리**: pandas

### 8.3 MA60_Slope (60일 이동평균선 기울기)
- **역할**: 60일 이동평균선의 기울기로 추세 강도 측정
- **계산법**: 정규화된 선형회귀 기울기
  - `calculate_normalized_linear_regression_slope(MA60, window=5)`
  - 5일 윈도우로 선형회귀 기울기를 계산하고, 현재 값으로 정규화하여 백분율로 변환
- **의미**: 양수면 상승 추세, 음수면 하락 추세. 값이 클수록 추세가 강함
- **사용 라이브러리**: pandas, numpy (선형회귀 계산)

---

## 9. 심리 지표

### 9.1 RSI_14 (상대 강도 지수 14일)
- **역할**: 매수세 확인. 주가는 빠졌어도 매수 심리가 50 근처에서 버티는지(Divergence) 확인
- **계산법**: `RSI(14)` - pandas-ta 라이브러리 사용
- **의미**: 0~100 범위, 50 이상이면 상승 압력, 50 이하면 하락 압력
- **사용 라이브러리**: pandas-ta

---

## 피처 카테고리별 요약

| 카테고리 | 피처 수 | 피처명 |
|---------|--------|--------|
| **기본 지표** | 2개 | log_mktcap, 52주_신고가_비율 |
| **추세 지표** | 1개 | ADX_14 |
| **이격도 지표** | 4개 | disparity_60, disparity_120, disparity_240, KOSPI_disparity_60 |
| **거래량 지표** | 1개 | OBV_Slope |
| **변동성 지표** | 4개 | ATRr_20, ATR_Ratio_Short, ATR_Ratio_Trend, KOSPI_변동성(1M) |
| **효율 지표** | 1개 | Eff_Ratio_10 |
| **수급 지표** | 1개 | RVOL |
| **위치 지표** | 3개 | Z_Score_20, Position_Range_60, MA60_Slope |
| **심리 지표** | 1개 | RSI_14 |
| **총계** | **18개** | |

---

## 사용 라이브러리

- **pandas-ta**: ATR, OBV, ADX, RSI 계산
- **pandas/numpy**: 이동평균, 표준편차, 이격도, 선형회귀 기울기 등 계산

---

## 주의사항

1. **데이터 품질**: 모든 피처는 충분한 과거 데이터(최소 250일)가 필요합니다.
2. **결측치 처리**: 계산 실패 시 `np.nan`으로 처리되며, 모델 학습 시 imputation이 적용됩니다.
3. **정규화**: 일부 피처는 범위가 다르므로 모델 학습 시 스케일링이 필요합니다.
4. **OBV 피처**: OBV는 OBV_Slope 계산에만 사용되며, 피처 리스트에는 포함되지 않습니다.

---

## 업데이트 이력

- 2024년: 초기 8개 피처 구성
- 2024년: 9개 피처 추가 (ATRr_20, ATR_Ratio_Short, ATR_Ratio_Trend, Eff_Ratio_10, RVOL, Z_Score_20, Position_Range_60, RSI_14, OBV_Slope)
- 2024년: KOSPI_변동성(1M), MA60_Slope 추가
- 2024년: OBV 피처 제거 (OBV_Slope 계산에만 사용), BB_Position 피처 제거
