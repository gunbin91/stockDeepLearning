# 피처(Features) 목록 및 설명

## 개요
이 문서는 주식 딥러닝 프로젝트에서 사용하는 모든 피처(Features)의 목록, 기능, 계산 방법을 정리한 것입니다.

**총 피처 수: 24개**

---

## 1. 기본 지표

### 1.1 log_mktcap (시가총액 로그)
- **역할**: 종목의 규모를 나타내는 지표. 로그 변환으로 왜도 완화
- **계산법**: `log(시가총액)`
- **의미**: 시가총액이 클수록 대형주, 작을수록 소형주
- **사용 라이브러리**: numpy

### 1.2 52주_신고가_비율
- **역할**: 현재가가 52주 최고가 대비 어느 정도 위치인지 측정
- **계산법**: `종가 ÷ 52주 최고가` (250일 롤링 최대값 사용)
- **의미**: 1.0에 가까울수록 신고가 근처, 낮을수록 하락세
- **사용 라이브러리**: pandas

---

## 2. 추세 지표

### 2.1 ADX_14 (Average Directional Index)
- **역할**: 추세의 강도를 측정하는 지표
- **계산법**: `ADX(14)` - pandas-ta 라이브러리 사용
- **의미**: 값이 높을수록 강한 추세, 낮을수록 횡보
- **사용 라이브러리**: pandas-ta

---

## 3. 이격도 지표

### 3.1 disparity_120 (120일 이격도)
- **역할**: 현재가가 120일 이동평균 대비 얼마나 떨어져 있는지 측정
- **계산법**: `(종가 ÷ 120일 이동평균) × 100`
- **의미**: 중장기 추세 판단에 사용
- **사용 라이브러리**: pandas

### 3.2 disparity_240 (240일 이격도)
- **역할**: 현재가가 240일 이동평균 대비 얼마나 떨어져 있는지 측정
- **계산법**: `(종가 ÷ 240일 이동평균) × 100`
- **의미**: 장기 추세 판단에 사용
- **사용 라이브러리**: pandas

### 3.3 KOSPI_disparity_20 (KOSPI 20일 이격도)
- **역할**: KOSPI 지수가 20일 이동평균 대비 얼마나 떨어져 있는지 측정
- **계산법**: `(KOSPI ÷ KOSPI 20일 이동평균) × 100`
- **의미**: 시장 전체의 단기 추세 판단
- **사용 라이브러리**: pandas

---

## 4. 가치 지표

### 4.1 PBR_log (PBR 로그 변환)
- **역할**: PBR(주가순자산비율)의 로그 변환으로 왜도 완화
- **계산법**: `log(PBR)` (PBR > 0인 경우만)
- **의미**: PBR의 절대값보다 변화율에 집중하여 종목 간 비교 용이
- **사용 라이브러리**: numpy

---

## 5. 거래량 지표

### 5.1 RVOL (상대 거래량, Relative Volume)
- **역할**: 평소보다 얼마나 더 거래량이 터졌는지 측정
- **계산법**: `오늘 거래량 / 거래량.rolling(20).mean()`
- **의미**: 
  - 값이 1.0보다 크면 평소보다 거래량이 많음 (관심 증가)
  - 값이 1.0보다 작으면 평소보다 거래량이 적음 (관심 감소)
- **활용**: 주가 피처만으로는 속임수(Fake)에 당할 수 있지만, 거래량 폭발이 동반된 주가 변동을 찾아내는 데 필수
- **사용 라이브러리**: pandas

---

## 6. 변동성 지표

### 6.1 KOSPI_변동성(1M) (KOSPI 변동성 1개월)
- **역할**: KOSPI 지수의 1개월(20일) 변동성 측정
- **계산법**: `KOSPI.rolling(20).std() ÷ KOSPI.rolling(20).mean()`
- **의미**: 시장 전체의 변동성 수준 파악
- **사용 라이브러리**: pandas

### 6.2 ATRr_5 (ATR 비율 5일)
- **역할**: 최근 1주일(5일) 기준 변동성 수준 측정
- **계산법**: `(ATR(5) / 종가) × 100`
- **의미**: 5일 기준 ATR을 종가로 나눈 비율로, 단기 변동성 수준을 나타냄
- **사용 라이브러리**: pandas-ta (ATR 계산), pandas

### 6.3 ATRr_20 (ATR 비율 20일)
- **역할**: 종목의 기초 체급(변동성 수준) 측정
- **계산법**: `(ATR(20) / 종가) × 100`
- **의미**: 20일 기준 ATR을 종가로 나눈 비율로, 종목의 기본적인 변동성 수준을 나타냄
- **사용 라이브러리**: pandas-ta (ATR 계산), pandas

### 6.4 ATRr_60 (ATR 비율 60일)
- **역할**: 3개월(60일) 기준 변동성 수준 측정
- **계산법**: `(ATR(60) / 종가) × 100`
- **의미**: 60일 기준 ATR을 종가로 나눈 비율로, 중장기 변동성 수준을 나타냄
- **사용 라이브러리**: pandas-ta (ATR 계산), pandas

### 6.5 Eff_Ratio_10 (효율성 비율 10일)
- **역할**: 가격 변동의 효율성을 측정하는 지표
- **계산법**: 
  1. change = `종가.diff(10).abs()` (10일 전과 현재의 차이의 절댓값)
  2. volatility = `종가.diff(1).abs().rolling(10).sum()` (1일 차이의 절댓값을 10일 합산)
  3. Eff_Ratio_10 = `change / (volatility + 1e-9)`
- **의미**: 
  - 값이 1.0에 가까울수록 가격 변동이 효율적임 (직선적인 움직임)
  - 값이 0에 가까울수록 가격 변동이 비효율적임 (횡보, 노이즈)
- **활용**: 추세의 강도와 방향성을 판단하는 데 유용. 높은 값은 강한 추세를 나타냄
- **사용 라이브러리**: pandas

---

## 7. 수급 지표

### 7.1 시총 회전율(1W) (시가총액 회전율 1주)
- **역할**: 1주일 기준 시가총액 대비 거래대금 비율 측정
- **계산법**: `(5일 평균 거래대금 / 시가총액) × 100`
- **의미**: 
  - 값이 클수록 거래가 활발함
  - 바닥권에서 회전율이 10~20% 이상 폭발한다는 건 "손바뀜(주포 교체)"이 일어났다는 강력한 신호
- **사용 라이브러리**: pandas

### 7.2 시총 회전율(3M) (시가총액 회전율 3개월)
- **역할**: 3개월 기준 시가총액 대비 거래대금 비율 측정
- **계산법**: `(60일 평균 거래대금 / 시가총액) × 100`
- **의미**: 중장기 기준으로 거래 활발도 측정
- **사용 라이브러리**: pandas

---

## 8. 위치 지표

### 8.1 Z_Score_20 (표준화 이격 20일)
- **역할**: 과매도 판단. 단기 평균 대비 통계적으로 얼마나 싼가
- **계산법**: `(종가 - 20일 평균) ÷ 20일 표준편차`
- **의미**: -2.0 근처면 통계적 바닥(반등 임박), 0 근처면 평균 수준
- **사용 라이브러리**: pandas

### 8.2 Position_Range_60 (Donchian 위치)
- **역할**: 추세 위치 측정. 지난 분기(60일)의 고가/저가 사이에서 현재 어디쯤인가
- **계산법**: `(종가 - 60일 최저) ÷ (60일 최고 - 60일 최저)`
- **의미**: 0~1 범위, 1.0에 가까울수록 신고가 돌파 직전, 0에 가까울수록 신저가 근처
- **사용 라이브러리**: pandas

### 8.3 MA120_Slope (120일 이동평균선 기울기)
- **역할**: 120일 이동평균선의 기울기로 중장기 추세 강도 측정
- **계산법**: 정규화된 선형회귀 기울기
  - `calculate_normalized_linear_regression_slope(MA120, window=5)`
  - 5일 윈도우로 선형회귀 기울기를 계산하고, 현재 값으로 정규화하여 백분율로 변환
- **의미**: 양수면 중장기 상승 추세, 음수면 중장기 하락 추세. 값이 클수록 추세가 강함
- **사용 라이브러리**: pandas, numpy (선형회귀 계산)

### 8.4 MA240_Slope (240일 이동평균선 기울기)
- **역할**: 240일 이동평균선의 기울기로 장기 추세 강도 측정
- **계산법**: 정규화된 선형회귀 기울기
  - `calculate_normalized_linear_regression_slope(MA240, window=5)`
  - 5일 윈도우로 선형회귀 기울기를 계산하고, 현재 값으로 정규화하여 백분율로 변환
- **의미**: 양수면 장기 상승 추세, 음수면 장기 하락 추세. 값이 클수록 추세가 강함
- **사용 라이브러리**: pandas, numpy (선형회귀 계산)

### 8.5 KOSPI_MA20_Slope (KOSPI 20일 이동평균선 기울기)
- **역할**: KOSPI 지수의 20일 이동평균선 기울기로 시장 전체의 단기 추세 강도 측정
- **계산법**: 정규화된 선형회귀 기울기
  - `calculate_normalized_linear_regression_slope(KOSPI_MA20, window=5)`
  - 5일 윈도우로 선형회귀 기울기를 계산하고, 현재 값으로 정규화하여 백분율로 변환
- **의미**: 양수면 시장 단기 상승 추세, 음수면 시장 단기 하락 추세. 값이 클수록 추세가 강함
- **사용 라이브러리**: pandas, numpy (선형회귀 계산)

---

## 9. 모멘텀 지표

### 9.1 RSI_Signal_Oscillator (RSI 신호 오실레이터)
- **역할**: RSI의 변화 추세를 측정. MACD 원리를 RSI에 적용
- **계산법**: `RSI_14 - RSI_14.rolling(9).mean()`
- **의미**: 
  - 값이 양수(+)면 RSI가 평균을 뚫고 올라가는 중(골든크로스) - 상승 모멘텀
  - 값이 음수(-)면 RSI가 평균 아래로 떨어지는 중(데드크로스) - 하락 모멘텀
- **활용**: 단순히 RSI가 50인 것보다, RSI가 상승 추세인지 하락 추세인지 구분하여 타이밍 잡기에 유리
- **사용 라이브러리**: pandas-ta (RSI 계산), pandas

---

## 피처 카테고리별 요약

| 카테고리 | 피처 수 | 피처명 |
|---------|--------|--------|
| **기본 지표** | 2개 | log_mktcap, 52주_신고가_비율 |
| **추세 지표** | 1개 | ADX_14 |
| **이격도 지표** | 3개 | disparity_120, disparity_240, KOSPI_disparity_20 |
| **가치 지표** | 1개 | PBR_log |
| **거래량 지표** | 1개 | RVOL |
| **변동성 지표** | 5개 | KOSPI_변동성(1M), ATRr_5, ATRr_20, ATRr_60, Eff_Ratio_10 |
| **수급 지표** | 2개 | 시총 회전율(1W), 시총 회전율(3M) |
| **위치 지표** | 5개 | Z_Score_20, Position_Range_60, MA120_Slope, MA240_Slope, KOSPI_MA20_Slope |
| **모멘텀 지표** | 1개 | RSI_Signal_Oscillator |
| **총계** | **22개** | |

---

## 사용 라이브러리

- **pandas-ta**: ATR, OBV, ADX, RSI 계산
- **pandas/numpy**: 이동평균, 표준편차, 이격도, 선형회귀 기울기, 로그 변환 등 계산

---

## 주의사항

1. **데이터 품질**: 모든 피처는 충분한 과거 데이터(최소 250일)가 필요합니다.
2. **결측치 처리**: 계산 실패 시 `np.nan`으로 처리되며, 모델 학습 시 imputation이 적용됩니다.
3. **정규화**: 일부 피처는 범위가 다르므로 모델 학습 시 스케일링이 필요합니다.
4. **OBV 피처**: OBV는 계산되지만 피처 리스트에는 포함되지 않습니다.
5. **RSI_14 피처**: RSI_14는 계산되지만 RSI_Signal_Oscillator 계산에만 사용됩니다.

---

## 업데이트 이력

- 2024년: 초기 8개 피처 구성
- 2024년: 9개 피처 추가 (ATRr_20, ATR_Ratio_Short, ATR_Ratio_Trend, Eff_Ratio_10, RVOL, Z_Score_20, Position_Range_60, RSI_14, OBV_Slope)
- 2024년: KOSPI_변동성(1M), MA60_Slope 추가
- 2024년: OBV 피처 제거 (OBV_Slope 계산에만 사용), BB_Position 피처 제거
- 2024년 12월: RSI_14 → RSI_Signal_Oscillator 변경, Relative_Strength_20 추가, 시총_회전율(1W) 추가
- 2024년 12월: 
  - **삭제된 피처**: 변동성(3M), RVOL, ATRr_20, 변동성(1W), ATR_Ratio_Trend, ATR_Ratio_Short, Relative_Strength_20, 시총_회전율(1W)
  - **추가된 피처**: 거래대금_log(1W), 거래대금_log(1M), HV변동성(3M), 표준화회전율(1W), 표준화회전율(1M), disparity_20
- 2024년 12월 (최신):
  - **삭제된 피처**: HV변동성(3M), 표준화회전율(1W), 거래대금_log(1W), disparity_60
  - **추가된 피처**: MA20_Slope
- 2024년 12월 (최신 업데이트):
  - **삭제된 피처**: MA60_Slope, OBV_Slope
  - **추가된 피처**: PBR_log, BBW_20_2, MA120_Slope, MA240_Slope, KOSPI_MA20_Slope
  - **변경된 피처**: KOSPI_disparity_60 → KOSPI_disparity_20
- 2024년 12월 (최신 업데이트 2):
  - **삭제된 피처**: HV변동성(1W), HV변동성(1M), Eff_Ratio_10, RSI_Signal_Oscillator
  - **추가된 피처**: 변동성(1W), 변동성(1M), 변동성(3M) (표준편차/평균 방식)
- 2024년 12월 (최신 업데이트 3):
  - **추가된 피처**: 시가총액_등급 (0:초소형주, 1:중소형주, 2:소형주, 3:중형주, 4:대형주)
- 2024년 12월 (최신 업데이트 4):
  - **삭제된 피처**: 변동성(1M), BBW_20_2, disparity_20, MA20_Slope, 표준화회전율(1M), 시가총액_등급, 거래대금_log(1M)
  - **추가된 피처**: RVOL, 시총 회전율(1W), 시총 회전율(3M), RSI_Signal_Oscillator, ATRr_20, ATR_Ratio_Short, ATR_Ratio_Trend, Eff_Ratio_10
- 2024년 12월 (최신 업데이트 5):
  - **삭제된 피처**: 변동성(1W), 변동성(3M)
- 2024년 12월 (최신 업데이트 6):
  - **추가된 피처**: ATRr_5, ATRr_60
- 2024년 12월 (최신 업데이트 7):
  - **삭제된 피처**: ATR_Ratio_Short, ATR_Ratio_Trend
