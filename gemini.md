# 백테스팅 및 가중치 최적화 개발 계획

## 개요

본 문서는 AI 기반 주식 추천 시스템의 성능을 객관적으로 검증하고, 핵심 로직인 `final_score`의 가중치를 데이터 기반으로 최적화하기 위한 개발 계획을 정의한다.

--- 

## 1단계: 핵심 로직 스크립트 개발 (백엔드)

**목표**: UI와 분리된 순수 파이썬 스크립트로 가중치 최적화 및 백테스팅의 핵심 기능을 구현한다.

### [x] 1.1. `weight_optimizer.py` 개발
-   **[x] 목적**: `final_score`를 구성하는 각 팩터(가치, 퀄리티, 모멘텀, ML예측 등)의 최적 가중치 조합을 탐색한다.
-   **[x] 데이터셋**: 훈련(2021-2022) / 검증(2023)으로 분리하여 사용.
-   **[x] 최적화 방법**: 그리드 서치를 통해 샤프 지수가 가장 높은 가중치 조합을 탐색.
-   **[x] 결과물**: 최적 가중치가 담긴 `optimal_weights.json` 파일을 생성.
-   **[x] 연동**: `ensemble.py`가 `optimal_weights.json` 파일을 읽어 가중치를 적용하도록 수정 완료.

### [x] 1.2. `backtest.py` 개발

-   **[x] 목적**: 확정된 ML 모델과 최적의 가중치를 사용하여, 시스템의 최종 성능을 객관적으로 검증한다.
-   **[x] 데이터셋**: 테스트 세트 (2024 ~ 현재)의 새로운 데이터를 사용한다.
-   **[x] 입력**: `stock_prediction_model_rf_upgraded.joblib`, `optimal_weights.json`
-   **[x] 시뮬레이션 로직**: `final_score` 기준 상위 5개 종목 매수 및 익절/손절/기간만료 규칙 적용.
-   **[x] 결과물**: 누적 수익률 그래프와 상세 성과 지표가 포함된 `backtest_report.html` 시각화 보고서를 생성한다.

--- 

## [ ] 2단계: 웹 UI 연동

**목표**: 1단계에서 개발된 핵심 기능을 사용자가 `Streamlit` 웹 앱에서 편리하게 사용할 수 있도록 통합한다.

-   **`app.py` 수정 사항**:
    1.  **[x] '백테스팅' 탭 신설**: 사이드바에 '백테스팅 리포트' 페이지 추가 완료.
    2.  **[x] 백테스팅 결과 표시**: `backtest_report.html` 파일을 읽어 웹 페이지에 표시 완료.
    3.  **[ ] '백테스팅 실행' 버튼 추가**: 웹 UI에서 `backtest.py` 스크립트를 직접 실행하는 기능.
    4.  **[ ] 실행 로직 연동**: 버튼 클릭 시, `backtest.py`의 로직을 실행하고 로딩 상태를 표시하는 기능.

--- 

## 시스템 실행 가이드

AI 기반 주식 추천 시스템을 실행하기 위한 단계별 가이드입니다.

1.  **DART API 키 설정**:
    *   `data_fetcher.py`, `train_model.py`, `weight_optimizer.py` 파일 내의 `DART_API_KEY` 변수에 발급받은 DART API 인증키를 입력해야 합니다.
    *   `DART_API_KEY = "여기에_발급받은_DART_인증키를_붙여넣으세요"` 부분을 본인의 키로 교체하세요.

2.  **기업 코드 맵 생성 (필요시 1회)**:
    *   `corp_code_map.csv` 파일이 없다면, `python make_corp_map.py` 스크립트를 실행하여 생성합니다. 이 파일은 DART API를 통해 기업 정보를 가져오는 데 필요합니다.

3.  **머신러닝 모델 학습**:
    *   `python train_model.py` 스크립트를 실행하여 머신러닝 모델을 학습시키고, 스케일러와 함께 `stock_prediction_model_rf_upgraded.joblib` 파일로 저장합니다. 이 과정은 데이터 수집 및 모델 학습에 시간이 다소 소요될 수 있습니다.

4.  **최적 가중치 탐색**:
    *   `python weight_optimizer.py` 스크립트를 실행하여 `final_score` 계산에 사용될 최적의 가중치 조합을 찾습니다. 이 과정은 백테스팅 시뮬레이션을 포함하므로 시간이 오래 걸릴 수 있습니다. 최적화된 가중치는 `optimal_weights.json` 파일로 저장됩니다.

5.  **백테스팅 실행 (선택 사항)**:
    *   `python backtest.py` 스크립트를 실행하여 확정된 ML 모델과 최적 가중치를 사용하여 시스템의 최종 성능을 검증합니다. 결과는 `backtest_report.html` 파일로 생성됩니다.

6.  **웹 애플리케이션 실행**:
    *   모든 준비가 완료되면, `streamlit run app.py` 명령어를 사용하여 웹 기반 주식 추천 시스템을 실행합니다.