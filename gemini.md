# 백테스팅 및 가중치 최적화 개발 계획

## 개요

본 문서는 AI 기반 주식 추천 시스템의 성능을 객관적으로 검증하고, 핵심 로직인 `final_score`의 가중치를 데이터 기반으로 최적화하기 위한 개발 계획을 정의한다.

--- 

## 1단계: 핵심 로직 스크립트 개발 (백엔드)

**목표**: UI와 분리된 순수 파이썬 스크립트로 가중치 최적화 및 백테스팅의 핵심 기능을 구현한다.

### [x] 1.1. `weight_optimizer.py` 개발
-   **[x] 목적**: `final_score`를 구성하는 각 팩터(가치, 퀄리티, 모멘텀, ML예측 등)의 최적 가중치 조합을 탐색한다.
-   **[x] 데이터셋**: 훈련(2021-2022) / 검증(2023)으로 분리하여 사용.
-   **[x] 최적화 방법**: 그리드 서치를 통해 샤프 지수가 가장 높은 가중치 조합을 탐색.
-   **[x] 결과물**: 최적 가중치가 담긴 `optimal_weights.json` 파일을 생성.
-   **[x] 연동**: `ensemble.py`가 `optimal_weights.json` 파일을 읽어 가중치를 적용하도록 수정 완료.

### [ ] 1.2. `backtest.py` 개발

-   **목적**: 확정된 ML 모델과 최적의 가중치를 사용하여, 시스템의 최종 성능을 객관적으로 검증한다.
-   **데이터셋**: 테스트 세트 (2024 ~ 현재)의 새로운 데이터를 사용한다.
-   **입력**: `stock_prediction_model_rf_upgraded.joblib`, `optimal_weights.json`
-   **시뮬레이션 로직**: `final_score` 기준 상위 5개 종목 매수 및 익절/손절/기간만료 규칙 적용.
-   **결과물**: 누적 수익률 그래프와 상세 성과 지표가 포함된 `backtest_report.html` 시각화 보고서를 생성한다.

--- 

## [ ] 2단계: 웹 UI 연동

**목표**: 1단계에서 개발된 핵심 기능을 사용자가 `Streamlit` 웹 앱에서 편리하게 사용할 수 있도록 통합한다.

-   **[ ] `app.py` 수정 사항**:
    1.  '백테스팅' 탭 신설.
    2.  '백테스팅 실행' 버튼 추가.
    3.  버튼 클릭 시, `backtest.py`의 로직을 실행하고 로딩 상태를 표시.
    4.  실행 완료 후, `backtest_report.html`의 내용을 분석하여 그래프와 핵심 지표를 웹페이지 내에 직접 렌더링.

--- 

## 협업 워크플로우

1.  **Gemini**: `gemini.md` 계획에 따라 스크립트를 개발한다.
2.  **사용자**: 개발된 스크립트를 실행하여 결과물(`json`, `html` 등)을 얻는다.
3.  **사용자**: Gemini에게 결과 파일 분석을 요청한다. (예: "`backtest_report.html` 파일을 읽고 분석해줘.")
4.  **Gemini**: 결과 파일을 읽고, 데이터 기반의 분석과 함께 다음 개선점을 구체적으로 제안한다.
5.  위 과정을 반복하며 시스템을 고도화한다.