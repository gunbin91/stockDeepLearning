# 백테스팅 및 가중치 최적화 개발 계획

## 개요

본 문서는 AI 기반 주식 추천 시스템의 성능을 객관적으로 검증하고, 핵심 로직인 `final_score`의 가중치를 데이터 기반으로 최적화하기 위한 개발 계획을 정의한다.

--- 

## 1단계: 핵심 로직 스크립트 개발 (백엔드)

**목표**: UI와 분리된 순수 파이썬 스크립트로 가중치 최적화 및 백테스팅의 핵심 기능을 구현한다.

### 1.1. `weight_optimizer.py` 개발

-   **목적**: `final_score`를 구성하는 각 팩터(가치, 퀄리티, 모멘텀, ML예측 등)의 최적 가중치 조합을 탐색한다.
-   **데이터셋**: 
    -   **훈련 세트 (2021-2022)**: ML 모델 학습에 사용.
    -   **검증 세트 (2023)**: 가중치 최적화를 수행할 데이터.
-   **최적화 방법**: 
    1.  **그리드 서치 (Grid Search)**: 사전에 정의된 가중치 후보군을 바탕으로 가능한 모든 조합을 생성한다.
    2.  각 조합에 대해 검증 세트 기간 동안 백테스팅을 수행한다.
    3.  **샤프 지수(Sharpe Ratio)**를 기준으로 가장 성과가 높았던 가중치 조합을 '최적해'로 선정한다.
-   **결과물**: 최적의 가중치 조합이 담긴 `optimal_weights.json` 파일을 생성한다.

### 1.2. `backtest.py` 개발

-   **목적**: 확정된 ML 모델과 최적의 가중치를 사용하여, 시스템의 최종 성능을 객관적으로 검증한다.
-   **데이터셋**:
    -   **테스트 세트 (2024 ~ 현재)**: 이전에 한 번도 사용되지 않은 데이터를 사용해 최종 성능을 측정한다.
-   **입력**: `stock_prediction_model_rf_upgraded.joblib`, `optimal_weights.json`
-   **시뮬레이션 로직**:
    1.  테스트 기간 동안 매일 `final_score`를 계산한다.
    2.  `final_score` 기준 상위 5개 종목을 매수 대상으로 선정한다.
    3.  아래의 매도 규칙을 적용한다.
        -   **익절**: +5% 상승 시
        -   **기간 만료**: 15 거래일 경과 시
        -   **손절**: -3% 하락 시
-   **결과물**: 
    -   주요 성과 지표(누적 수익률, 연환산 수익률, MDD, 샤프 지수, 승률)를 터미널에 출력한다.
    -   누적 수익률 그래프(KOSPI 비교 포함)와 상세 지표가 포함된 `backtest_report.html` 시각화 보고서를 생성한다.

--- 

## 2단계: 웹 UI 연동

**목표**: 1단계에서 개발된 핵심 기능을 사용자가 `Streamlit` 웹 앱에서 편리하게 사용할 수 있도록 통합한다.

-   **`app.py` 수정 사항**:
    1.  '백테스팅' 탭 신설.
    2.  '백테스팅 실행' 버튼 추가.
    3.  버튼 클릭 시, `backtest.py`의 로직을 실행하고 로딩 상태를 표시.
    4.  실행 완료 후, `backtest_report.html`의 내용을 분석하여 그래프와 핵심 지표를 웹페이지 내에 직접 렌더링.

--- 

## 협업 워크플로우

1.  **Gemini**: `gemini.md` 계획에 따라 스크립트(`weight_optimizer.py`, `backtest.py`)를 개발한다.
2.  **사용자**: 개발된 스크립트를 실행하여 결과물(`optimal_weights.json`, `backtest_report.html`)을 얻는다.
3.  **사용자**: Gemini에게 결과 파일 분석을 요청한다. (예: "`backtest_report.html` 파일을 읽고 분석해줘.")
4.  **Gemini**: 결과 파일을 읽고, 데이터 기반의 분석과 함께 다음 개선점(예: 피처 엔지니어링, 손절 로직 변경 등)을 구체적으로 제안한다.
5.  위 과정을 반복하며 시스템을 고도화한다.
